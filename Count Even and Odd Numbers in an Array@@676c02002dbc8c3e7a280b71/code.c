#include <stdio.h>

int main(){
    int a;
    int evencount=0;
    int oddcount=0;
    scanf("%d",&a);
    int arr[n];
    for(int i=0;i<a;i++){
        scanf("%d",&arr[n]);
        if(arr[i]%2==0){
            evencount++;
        }else{
            oddcount++;
        }
        printf("%d %d",evencount,oddcount);
    }
    return 0;
}